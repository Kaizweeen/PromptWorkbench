# Copy to .env.local and fill in. .env.local is gitignored and never committed.
# The key is read only in server-side route handlers; it never reaches the browser.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the local database location.
# WORKBENCH_DB=./data/workbench.db
